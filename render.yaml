services:
  - type: web
    name: ytb-transcript-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 8 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.4
